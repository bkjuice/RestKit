<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>RestKit.TestLib</RootNamespace>
    <AssemblyName>RestKit.TestLib</AssemblyName>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>

  <ItemGroup>
    <!-- Dead code (zero references anywhere in the solution) built on System.Web.Http/SelfHost,
         which has no modern-.NET equivalent. Excluded rather than ported. -->
    <Compile Remove="TestHttpServer.cs" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RestKit\RestKit.csproj" />
  </ItemGroup>

</Project>
